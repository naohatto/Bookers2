<%= render 'books/usernewbook', newbook: @book, user: @user %>

<% @users.each do |user| %>
  <div>
    <p>投稿ユーザー画像：<%= image_tag user.get_profile_image(100,100) %></p>
    <p><%= user.name %></p>
    <p><%= link_to "Show", user_path(user.id) %></p>
  </div>
<% end %>
<%= paginate @users %>